task-1:
1. Create an html document with three paragraphs.
2. apply CSS style to each paragraph using inline, internal and external syles.
task-2:
1.design a webpage with a heading, paragraphs and a list 
2. Apply CSS styles to change the text color, alignment, font size, font family and font style for different elements. 
task-3:
1.create an html document with various elements,including heading, paragraphs, and divs 
2. Use different CSS selectors(id, class, universal, tag) to apply unique styles to specific elements. 
task-4:
1. Design a webpage with multiple sections/divs 
2. Apply CSS styles to set different background colors, images, repets, positions, and sizes for each section 
task-5:
1. Create a webpage with various element, such as divs, paragraphs, and images. 
2. Apply CSS styles to add borders with different widths, colos, border-radius, and box-shadow effects to the element